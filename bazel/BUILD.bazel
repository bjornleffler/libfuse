load("@rules_cc//cc:cc_library.bzl", "cc_library")

cc_library(
    name = "fuse_config",
    hdrs = [
        "fuse_config.h",
        "libfuse_config.h",
    ],
    includes = ["."],
    visibility = ["//visibility:public"],
)
